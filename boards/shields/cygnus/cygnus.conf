# Enable ZMK as a Bluetooth keyboard
CONFIG_ZMK_BLE=y

# Enable split keyboard support (if using wireless left/right halves)
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_BLE=y

# Optional: Increase Bluetooth signal strength if experiencing lag/dropouts
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y